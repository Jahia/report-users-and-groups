query GetReportStatus($csvRootPath: String!) {
    reportUsersAndGroups {
        isGenerating
        files(csvRootPath: $csvRootPath) {
            path
            downloadUrl
            createdAt
        }
    }
}
